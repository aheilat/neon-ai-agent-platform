import { Badge } from "@/components/ui/badge";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { trpc } from "@/lib/trpc";
import { Activity, BarChart3, CheckCircle2, MessageSquareText, ShieldCheck, TrendingUp, UsersRound } from "lucide-react";

export default function Analytics() {
  const { data: stats, isLoading } = trpc.analytics.overview.useQuery();
  const total = stats?.conversations ?? 0;
  const resolutionRate = total ? Math.round(((stats?.resolved ?? 0) / total) * 100) : 0;
  const escalationRate = total ? Math.round(((stats?.escalated ?? 0) / total) * 100) : 0;
  const items = [
    { label: "إجمالي المحادثات", value: total, icon: MessageSquareText, color: "text-cyan-300", bg: "bg-cyan-300/10" },
    { label: "الرسائل المتبادلة", value: stats?.messages ?? 0, icon: Activity, color: "text-violet-300", bg: "bg-violet-300/10" },
    { label: "معدل الحل", value: `${resolutionRate}%`, icon: CheckCircle2, color: "text-lime-300", bg: "bg-lime-300/10" },
    { label: "معدل التصعيد", value: `${escalationRate}%`, icon: TrendingUp, color: "text-amber-300", bg: "bg-amber-300/10" },
  ];
  const distribution = [
    { label: "نشطة", value: stats?.active ?? 0, color: "bg-cyan-300" },
    { label: "تم الحل", value: stats?.resolved ?? 0, color: "bg-lime-300" },
    { label: "تصعيد", value: stats?.escalated ?? 0, color: "bg-amber-300" },
  ];
  return <div className="mx-auto max-w-[1400px] space-y-7 pb-12" dir="rtl"><header><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300/80" dir="ltr">Insights / 05</p><h1 className="mt-2 text-3xl font-semibold tracking-tight text-white">التحليلات <span className="text-slate-500">/ Analytics</span></h1><p className="mt-2 max-w-2xl text-sm leading-6 text-slate-400">رؤية عملية لأداء وكلائك، مع مؤشرات مبنية على المحادثات الفعلية داخل مساحة العمل.</p></header><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{items.map(item => <Card key={item.label} className="border-white/10 bg-[#0b1728]/90"><CardContent className="p-5"><div className="flex items-start justify-between"><div><p className="text-xs text-slate-400">{item.label}</p><p className="mt-3 text-3xl font-semibold text-white">{isLoading ? "—" : item.value}</p></div><div className={`flex h-10 w-10 items-center justify-center rounded-xl ${item.bg} ${item.color}`}><item.icon className="h-5 w-5" /></div></div></CardContent></Card>)}</div><div className="grid gap-5 xl:grid-cols-[1.1fr_0.9fr]"><Card className="border-white/10 bg-[#0b1728]/90"><CardHeader className="flex flex-row items-center justify-between border-b border-white/10 pb-5"><div><CardTitle className="text-base text-white">توزيع حالات المحادثات</CardTitle><p className="mt-1 text-xs text-slate-400">مقياس مباشر من صندوق المحادثات</p></div><BarChart3 className="h-5 w-5 text-cyan-300" /></CardHeader><CardContent className="space-y-6 p-6">{distribution.map(item => <div key={item.label}><div className="mb-2 flex items-center justify-between text-xs"><span className="text-slate-300">{item.label}</span><span className="text-slate-500">{item.value}</span></div><div className="h-3 overflow-hidden rounded-full bg-white/[0.06]"><div className={`h-full rounded-full ${item.color} transition-all`} style={{ width: total ? `${Math.max((item.value / total) * 100, item.value ? 4 : 0)}%` : "0%" }} /></div></div>)}{!total && <div className="rounded-2xl border border-dashed border-white/10 bg-white/[0.02] p-6 text-center text-xs text-slate-500">ستظهر الاتجاهات بعد بدء استقبال المحادثات.</div>}</CardContent></Card><Card className="border-white/10 bg-[#0b1728]/90"><CardHeader className="border-b border-white/10 pb-5"><CardTitle className="text-base text-white">جودة الاستجابة</CardTitle><p className="mt-1 text-xs text-slate-400">إشارات قابلة للقياس بدون اختلاق تقييمات للعملاء</p></CardHeader><CardContent className="space-y-4 p-6"><div className="flex items-start gap-4 rounded-2xl border border-lime-300/15 bg-lime-300/[0.06] p-4"><ShieldCheck className="mt-0.5 h-5 w-5 shrink-0 text-lime-300" /><div><p className="text-sm font-medium text-white">مؤشر موثوق</p><p className="mt-1 text-xs leading-5 text-slate-400">يُقاس الأداء حالياً من معدل الحل والتصعيد. يمكن إضافة تقييمات بشرية بعد ربط نظام خدمة العملاء.</p></div></div><div className="grid grid-cols-2 gap-3"><div className="rounded-2xl border border-white/[0.08] bg-white/[0.03] p-4"><p className="text-xs text-slate-500">حل آلي</p><p className="mt-2 text-2xl font-semibold text-lime-300">{resolutionRate}%</p></div><div className="rounded-2xl border border-white/[0.08] bg-white/[0.03] p-4"><p className="text-xs text-slate-500">تحويل للبشر</p><p className="mt-2 text-2xl font-semibold text-amber-300">{escalationRate}%</p></div></div><div className="flex items-center justify-between rounded-2xl border border-white/[0.08] p-4"><div className="flex items-center gap-3"><UsersRound className="h-4 w-4 text-cyan-300" /><span className="text-xs text-slate-300">جميع المؤشرات معزولة حسب مساحة العمل</span></div><Badge className="border-cyan-300/20 bg-cyan-300/10 text-[10px] text-cyan-200">Tenant-safe</Badge></div></CardContent></Card></div></div>;
}
